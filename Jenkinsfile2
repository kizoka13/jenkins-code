pipeline{
    agent any
    tools{
        maven 'M2_HOME'
    }
    stages{
        satge('maven commands'){
            steps{
                sh 'mvn clean compile install package'
            }
        }
    }
}